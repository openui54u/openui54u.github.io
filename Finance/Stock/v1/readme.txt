Advanced Market Analyst
- Place files on a static host (GitHub Pages recommended).
- Provide API keys in the input box in the format:
  finnhub=YOURKEY;alpha=YOURKEY
- Use symbol notation like ASML.AS for Euronext.
- Backtest runs a simple SMA50/SMA200 long-only simulation.
